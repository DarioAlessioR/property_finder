<% @properties.each do |property| %>
    <div class="col-3">
        <%= property_thumbnail(property) %>
        <br>
        <%= property.name %>
        <p><%= property.price %>" $"<p>
    </div>
<% end %>


